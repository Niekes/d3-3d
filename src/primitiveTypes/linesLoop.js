import {rotateRxRyRz}   from '../rotation.js';
import {project}        from '../projection.js';

export function linesLoop(data, projection, alpha, beta, gamma, origin, scale, distance){
    for (var i = data.length - 1; i >= 0; i--) {}
    return data;
}
